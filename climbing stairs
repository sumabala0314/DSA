class Solution {
    public int climbStairs(int n) {
        if (n==1)
            return 1;
        if(n==2)
            return 2;
        int i=climbStairs(n-1);
        int j=climbStairs(n-2);
        return i+j;
    }
}
